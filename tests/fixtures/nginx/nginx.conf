events {}

http {
    server {
        listen 8443 ssl;
        server_name dagmar.hcasc.cz;
        ssl_certificate /etc/nginx/tls/cert.pem;
        ssl_certificate_key /etc/nginx/tls/key.pem;
        include /etc/nginx/conf.d/dagmar-location.conf;
    }
}
